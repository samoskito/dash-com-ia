**Tag** — monospace filter chip for campaign / adset / ad / UTM values.

```jsx
<Tag prefix="campanha:" active onRemove={() => {}}>black-friday</Tag>
<Tag prefix="ad_id:">23859441</Tag>
```

Mono by design (these are data values). `active` applies the brand tint; pass `onRemove` to make it dismissible.
